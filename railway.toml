[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn app:app --workers 2 --threads 2 --worker-class gthread --timeout 120 --bind 0.0.0.0:$PORT"
healthcheckPath = "/healthz"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
